# Shell builtins for the embedded brush shell.
#
# Two layers live here:
#  * the POSIX/bash builtins (`cd`, `echo`, `test`, `printf`, `read`, …), a
#    locally-patched fork of the upstream `brush-builtins` crate, and
#  * the in-process command-line utilities the shell ships (`cat`, `grep`,
#    `sed`, `ls`, `jq`, `ps`, `top`, `kill`, …), one module per command.
#
# The utilities used to be ~50 separate vendored crates driven through a
# thread-local I/O shim; they now run as ordinary builtins against the explicit
# `host::Host` view of the shell (see `src/host.rs`). See LICENSE for the
# third-party notices that come with the ported code.
[package]
name = "pi-builtins"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
rust-version = "1.88.0"
description = "Builtins and in-process command-line utilities for the embedded brush shell"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "pi_builtins"
path = "src/lib.rs"
bench = false

[dependencies]
brush-core = "^0.5.0"
brush-parser = "^0.4.0"
cfg-if = "1.0.4"
chrono = "0.4.44"
clap = { version = "4.6.0", features = ["cargo", "color", "derive", "env", "wrap_help"] }
fancy-regex = "0.17"
futures = "0.3.32"
itertools = "0.14.0"
parking_lot = "0.12.5"
strum = "0.28.0"
strum_macros = "0.28.0"
thiserror = "2.0.18"
tracing = "0.1.44"

# ── Utility builtin dependencies ──────────────────────────────────────────────
ansi-width = "0.1.0"
argmax = "0.3.1"
bigdecimal = "0.4"
binary-heap-plus = "0.5.0"
bstr = "1.12.0"
bytecount = { version = "0.6.8", features = ["runtime-dispatch-simd"] }
codesnake = "0.2"
compare = "0.1.0"
faccess = "0.2.4"
filetime = "0.2.23"
flume = "0.11"
foldhash = "0.2.0"
fs_extra = "1.3.0"
glob = "0.3.1"
globset = "0.4"
grep-cli = "0.1"
grep-matcher = "0.1"
grep-pcre2 = "0.1"
grep-printer = "0.3"
grep-regex = "0.1"
grep-searcher = "0.1"
hex = "0.4.3"
hifijson = "0.2.0"
hostname = "0.4"
ignore = "0.4"
indicatif = "0.18.0"
jaq-core = "2.1.1"
jaq-json = "1.1.1"
jaq-std = "2.1.0"
jiff = { version = "0.2.18", features = ["tzdb-bundle-platform"] }
lscolors = { version = "0.21.0", default-features = false, features = ["gnu_legacy"] }
memchr = "2.7.4"
nom = "8.0.0"
num-bigint = "0.4"
num-traits = "0.2"
onig = { version = "6.4", default-features = false }
os_display = "0.1.4"
parse_datetime = "0.14"
pi-walker = { path = "../pi-walker" }
platform-info = "2.0.3"
rand = { version = "0.10.0", features = ["std_rng"] }
rayon = "1.10"
regex = "1.11"
rustc-hash = "2.1.1"
self_cell = "1.0.4"
serde_json = "1"
smallvec.workspace = true
similar = "3.1.0"
tempfile = "3.15.0"
tokio-util = "0.7"
xutf.workspace = true
libc = "0.2.172"
memmap2 = "0.9"
uutils_term_grid = "0.8"
yansi = "1.0.1"

[dependencies.uucore]
version = "0.8.0"
default-features = false
features = [
  "backup-control",
  "checksum",
  "colors",
  "encoding",
  "entries",
  "extendedbigdecimal",
  "fast-inc",
  "format",
  "fs",
  "fsext",
  "hardware",
  "i18n-charmap",
  "i18n-collator",
  "i18n-datetime",
  "i18n-decimal",
  "libc",
  "lines",
  "mode",
  "parser",
  "pipes",
  "process",
  "quoting-style",
  "ranges",
  "ringbuffer",
  "signals",
  "sum",
  "time",
  "version-cmp",
  "wide",
  "update-control",
]

[target."cfg(any(unix, windows))".dependencies.tokio]
version = "1.50.0"
features = ["io-util", "macros", "process", "rt", "rt-multi-thread", "signal", "sync"]

[target.'cfg(target_family = "wasm")'.dependencies.tokio]
version = "1.52.1"
features = ["io-util", "macros", "rt"]

[target.'cfg(any(target_os = "linux", target_os = "android"))'.dependencies]
procfs = "0.18.0"

[target."cfg(unix)".dependencies]
dns-lookup = "3.0.0"
nix = { version = "0.31.2", features = ["fs", "poll", "process", "resource", "signal", "term", "user"] }
notify = { version = "=8.2.0", features = ["macos_kqueue"] }
rlimit = "0.11.0"
rustix = { version = "1.1.4", features = ["fs", "process", "time"] }
uucore = { version = "0.8.0", features = ["fsxattr", "safe-traversal"] }

[target."cfg(windows)".dependencies]
notify = "=8.2.0"
rustix = { version = "1.1.4", features = ["fs", "process", "time"] }
windows-sys = { version = "0.61", features = [
  "Win32_Foundation",
  "Win32_Networking_WinSock",
  "Win32_Storage_FileSystem",
  "Win32_System_IO",
  "Win32_System_SystemInformation",
  "Win32_System_WindowsProgramming",
] }

[dev-dependencies]
anyhow = "1.0.102"
pretty_assertions = { version = "1.4.1", features = ["unstable"] }
tempfile = "3"

[features]
default = ["base", "utils"]

# Every upstream POSIX/bash builtin.
base = [
  "builtin.alias",
  "builtin.bg",
  "builtin.bind",
  "builtin.break",
  "builtin.builtin",
  "builtin.caller",
  "builtin.cd",
  "builtin.colon",
  "builtin.command",
  "builtin.compgen",
  "builtin.compopt",
  "builtin.complete",
  "builtin.continue",
  "builtin.declare",
  "builtin.dirs",
  "builtin.dot",
  "builtin.echo",
  "builtin.enable",
  "builtin.eval",
  "builtin.exec",
  "builtin.exit",
  "builtin.export",
  "builtin.false",
  "builtin.fc",
  "builtin.fg",
  "builtin.getopts",
  "builtin.hash",
  "builtin.help",
  "builtin.history",
  "builtin.jobs",
  "builtin.kill",
  "builtin.let",
  "builtin.mapfile",
  "builtin.popd",
  "builtin.printf",
  "builtin.pushd",
  "builtin.pwd",
  "builtin.read",
  "builtin.return",
  "builtin.set",
  "builtin.shift",
  "builtin.shopt",
  "builtin.suspend",
  "builtin.test",
  "builtin.times",
  "builtin.trap",
  "builtin.true",
  "builtin.type",
  "builtin.ulimit",
  "builtin.umask",
  "builtin.unalias",
  "builtin.unset",
  "builtin.wait",
]

"builtin.alias" = []
"builtin.bg" = []
"builtin.bind" = []
"builtin.break" = []
"builtin.builtin" = []
"builtin.caller" = []
"builtin.cd" = []
"builtin.colon" = []
"builtin.command" = []
"builtin.compgen" = []
"builtin.complete" = []
"builtin.compopt" = []
"builtin.continue" = []
"builtin.declare" = []
"builtin.dirs" = []
"builtin.dot" = []
"builtin.echo" = []
"builtin.enable" = []
"builtin.eval" = []
"builtin.exec" = []
"builtin.exit" = []
"builtin.export" = []
"builtin.false" = []
"builtin.fc" = []
"builtin.fg" = []
"builtin.getopts" = []
"builtin.hash" = []
"builtin.help" = []
"builtin.history" = []
"builtin.jobs" = []
# The non-unix existence probe scans the process table.
"builtin.kill" = ["util.procs"]
"builtin.let" = []
"builtin.mapfile" = []
"builtin.popd" = []
"builtin.printf" = []
"builtin.pushd" = []
"builtin.pwd" = []
"builtin.read" = []
"builtin.return" = []
"builtin.set" = []
"builtin.shift" = []
"builtin.shopt" = []
"builtin.suspend" = []
"builtin.test" = []
"builtin.times" = []
"builtin.trap" = []
"builtin.true" = []
"builtin.type" = []
"builtin.ulimit" = []
"builtin.umask" = []
"builtin.unalias" = []
"builtin.unset" = []
"builtin.wait" = []

# ── Utility builtins ──────────────────────────────────────────────────────────
# One feature per ported utility so a single utility can be built (and checked)
# in isolation. `utils` turns on the whole set and is part of `default`.
"util.b2sum" = ["util.cksum"]
"util.base32" = []
"util.base64" = ["util.base32"]
"util.basename" = []
"util.cat" = []
"util.cksum" = []
"util.cmp" = []
"util.comm" = []
"util.cut" = []
"util.date" = []
"util.diff" = []
"util.dirname" = []
"util.fd" = []
"util.find" = []
"util.grep" = []
"util.head" = []
"util.hostname" = []
"util.jq" = []
"util.ln" = []
"util.ls" = []
"util.md5sum" = ["util.cksum"]
"util.mkdir" = []
"util.mktemp" = []
"util.combine" = []
"util.errno" = []
"util.ifne" = []
"util.isutf8" = []
"util.sponge" = []
"util.ts" = []
"util.mv" = []
"util.nproc" = []
"util.paste" = []
"util.nohup" = ["util.procs"]
"util.pgrep" = ["util.proc-match"]
"util.pidwait" = ["util.proc-match"]
"util.pkill" = ["util.proc-match"]
# Shared process-matching engine behind pgrep/pkill/pidwait.
"util.proc-match" = ["builtin.kill", "util.procs"]
# Shared process-table snapshot behind ps/top/pgrep/pkill/pidwait/kill.
"util.procs" = []
"util.ps" = ["util.procs"]
"util.sleep" = []
"util.timeout" = ["util.procs"]
"util.top" = ["util.procs"]
"util.printenv" = []
"util.readlink" = []
"util.realpath" = []
# Shares the PCRE2 JIT probe with `grep`.
"util.rg" = ["util.grep"]
"util.rm" = []
"util.sed" = []
"util.seq" = []
"util.sha1sum" = ["util.cksum"]
"util.sha224sum" = ["util.cksum"]
"util.sha256sum" = ["util.cksum"]
"util.sha384sum" = ["util.cksum"]
"util.sha512sum" = ["util.cksum"]
"util.sort" = []
"util.stat" = []
"util.tac" = []
"util.tail" = ["util.tac"]
"util.tee" = []
"util.touch" = []
"util.tr" = []
"util.truncate" = []
"util.uname" = []
"util.uniq" = []
"util.wc" = []
"util.which" = []
"util.whoami" = []
"util.xargs" = []
"util.yes" = []

utils = [
  "util.b2sum",
  "util.base32",
  "util.base64",
  "util.basename",
  "util.cat",
  "util.cksum",
  "util.cmp",
  "util.comm",
  "util.cut",
  "util.date",
  "util.diff",
  "util.dirname",
  "util.fd",
  "util.find",
  "util.grep",
  "util.head",
  "util.hostname",
  "util.jq",
  "util.ln",
  "util.ls",
  "util.md5sum",
  "util.mkdir",
  "util.mktemp",
  "util.combine",
  "util.errno",
  "util.ifne",
  "util.isutf8",
  "util.sponge",
  "util.ts",
  "util.mv",
  "util.nproc",
  "util.paste",
  "util.nohup",
  "util.pgrep",
  "util.pidwait",
  "util.pkill",
  "util.proc-match",
  "util.procs",
  "util.ps",
  "util.sleep",
  "util.timeout",
  "util.top",
  "util.printenv",
  "util.readlink",
  "util.realpath",
  "util.rg",
  "util.rm",
  "util.sed",
  "util.seq",
  "util.sha1sum",
  "util.sha224sum",
  "util.sha256sum",
  "util.sha384sum",
  "util.sha512sum",
  "util.sort",
  "util.stat",
  "util.tac",
  "util.tail",
  "util.tee",
  "util.touch",
  "util.tr",
  "util.truncate",
  "util.uname",
  "util.uniq",
  "util.wc",
  "util.which",
  "util.whoami",
  "util.xargs",
  "util.yes",
]

# Deliberately NOT `lints.workspace = true`: most of this crate is ported
# third-party code (brush's builtins, uutils, jaq), and holding 70k lines of
# it to the workspace's pedantic/nursery bar would bury real warnings. rustc
# warnings still apply and are kept at zero.
[lints.clippy]
all = { level = "allow", priority = -1 }
nursery = { level = "allow", priority = -1 }
pedantic = { level = "allow", priority = -1 }
cargo = { level = "allow", priority = -1 }

[lints.rust]
unfulfilled_lint_expectations = { level = "allow", priority = -1 }
