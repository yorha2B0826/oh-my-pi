These files have uncommitted changes in a git repository:

{{fileList}}

The user only wants to stage: {{instruction}}

Reply with the exact path of every file that matches, one per line, copied verbatim from the list. If none match, reply "none".
