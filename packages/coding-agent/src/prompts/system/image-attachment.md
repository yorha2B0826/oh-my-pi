The user attached an image as `[Image #{{index}}]` in their visible message.

Source path: `{{path}}`

The attached image was loaded from this file on disk. Use the source path directly with file tools (for example `read {{path}}`) when you need the original file or want to act on it.
