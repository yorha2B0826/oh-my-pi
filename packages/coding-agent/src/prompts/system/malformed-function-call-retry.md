<system-injection>
The provider rejected your last turn: the function call was malformed and never executed. Function calls written as text (code fences, `call:default_api:…`, `print(default_api.…)`, tool_code blocks) are NOT calls.
Re-issue the intended call now through the native function-calling interface, with arguments as a JSON object matching the tool schema. No prose, no transcription.
Attempt #{{retryCount}}/{{maxRetries}}
</system-injection>
