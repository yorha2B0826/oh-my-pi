import type { CompiledCompatRules } from "./types";

/** Typed declaration for the generated rules.json (see `scripts/compat-compiler`). */
declare const rules: CompiledCompatRules;
export default rules;
