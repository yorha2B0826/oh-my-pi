auth "lm-studio" {
	name "LM Studio (Local OpenAI-compatible)"
	login "api-key" {
		prompt "Optional: Paste LM Studio API key (to customize endpoint URL, set LM_STUDIO_BASE_URL env var)" placeholder="lm-studio-local"
		empty-fallback "lm-studio-local"
	}
}
