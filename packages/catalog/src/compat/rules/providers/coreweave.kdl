// Provider-wire compat for "coreweave"; regenerated from the frozen census using deployment contract selectors.

provider "coreweave" {
	default-model "openai/gpt-oss-120b"
	env "COREWEAVE_API_KEY" "WANDB_API_KEY"
	dynamic-models-authoritative #true
	discovery label="CoreWeave Serverless Inference"

	thinking-mode "effort"
	class "unknown" {
		thinking-efforts "minimal" "low" "medium" "high" "xhigh"
	}
	// residue: taxonomy ranks and exact globs do not isolate these models.
	models "zai-org/GLM-5.1" {
		thinking-efforts "minimal" "low" "medium" "high" "xhigh"
	}
	// residue: taxonomy ranks and exact globs do not isolate these models.
	models "zai-org/GLM-5.2" {
		thinking-efforts "minimal" "low" "medium" "high" "max"
	}
}
