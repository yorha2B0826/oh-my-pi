// Provider-wire compat for "novita"; regenerated from the frozen census using deployment contract selectors.

provider "novita" {
	thinking-mode "effort"
	class "glm" {
		family "turbo" {
			thinking-efforts "minimal" "low" "medium" "high" "xhigh"
		}
	}
	class "unknown" {
		thinking-efforts "minimal" "low" "medium" "high" "xhigh"
	}
	// residue: taxonomy ranks and exact globs do not isolate these models.
	models "deepseek/deepseek-r1/community" {
		requires-reasoning-content-for-all-assistant-turns #true
		thinking-efforts "high" "max"
	}
	// residue: taxonomy ranks and exact globs do not isolate these models.
	models "deepseek/deepseek-v3.*" {
		requires-reasoning-content-for-all-assistant-turns #true
	}
	// residue: taxonomy ranks and exact globs do not isolate these models.
	models "qwen/qwen3-235b-a22b-thinking-2507" {
		thinking-requires-effort #true
	}
	// residue: taxonomy ranks and exact globs do not isolate these models.
	models "zai-org/glm-4*" "zai-org/glm-5" "zai-org/glm-5.1" {
		thinking-efforts "minimal" "low" "medium" "high" "xhigh"
	}
	// residue: taxonomy ranks and exact globs do not isolate these models.
	models "zai-org/glm-5.2" {
		thinking-efforts "minimal" "low" "medium" "high" "max"
	}
}
